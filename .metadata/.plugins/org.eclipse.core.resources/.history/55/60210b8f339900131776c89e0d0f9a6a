
public class BinaryTree {
	BinaryNode<Integer, String> root;
	
	public void addNode(int key, String value) {
		BinaryNode<Integer, String> newNode = new BinaryNode<Integer, String>(key, value);
		if (this.root == null) {
			root = newNode;
		} else {
			BinaryNode<Integer, String> focusNode = root;
			BinaryNode<Integer, String> parent;
			while (true) {
				parent = focusNode;
				if (key < focusNode.getKey()) {
					focusNode = focusNode.getLeft();
					if (focusNode == null) {
						parent.setLeft(newNode);
						return;
					}
				} else {
					focusNode = focusNode.getRight();
					if(focusNode == null) {
						parent.setRight(newNode);
						return;
					}
				}
			}
		}
	}
	
	public String inorderTraverse() {
		return inorderTraverseHelper(root);
	}
	
	/**
	 * Traverse the tree and retern a string of the traverse
	 * @param focusNode
	 * @return String with the tree traversed by inorder
	 */
	private String inorderTraverseHelper(BinaryNode<Integer, String> focusNode) {
		String result = "";
		if (focusNode.getLeft() != null) inorderTraverseHelper(focusNode.getLeft());
		result += focusNode.getValue();
		if (focusNode.getRight() != null) inorderTraverseHelper(focusNode.getRight());
		return result;
	}
	
	public String preorderTraverse() {
		return preorderTraverseHelper(root);
	}
	
	private String preorderTraverseHelper(BinaryNode<Integer, String> focusNode) {
		String result = "";
		result += focusNode.getValue();
		if (focusNode.getLeft() != null) preorderTraverseHelper(focusNode.getLeft());
		if (focusNode.getRight() != null) preorderTraverseHelper(focusNode.getRight());
		return result;
	}
	
	/**
	 * Overrides the toString() method
	 * Returns the tree inorder by default
	 */
	@Override public String toString() {
		return inorderTraverseHelper(root);
	}
	
	
}
